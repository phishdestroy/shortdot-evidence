# Package Directory

Raw data exports from scan pipeline.

| Path | Contents |
|---|---|
| `raw_data/` | Gzip-compressed JSONL from NetAPI and HTTP scan stages |

Files in `raw_data/` are excluded from git (see `.gitignore`) due to size. Available on request for verified researchers.
