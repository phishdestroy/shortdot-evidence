# IOC Directory

Confirmed malicious domains across ShortDot zones.

| File | Description |
|---|---|
| `domains_all_malicious.txt` | All confirmed malicious domains |
| `domains_high.txt` | HIGH severity only (phishing, drain, carding) |
| `indicators.csv` | Full IOC table with classification and metadata |

Populated by `scan/fetch_new.py` + manual review. Updated daily.
